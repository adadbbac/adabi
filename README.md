# الأدب العربي في البكالوريا 2.0 — الرفع إلى GitHub

هذه هي النسخة المصححة والجاهزة للرفع التجريبي.

مهم:
- الدفعات الثلاث مفلطحة بالكامل: لا توجد مجلدات داخلها.
- فك ضغط كل دفعة ثم ارفع **محتوياتها** إلى نفس المستودع `adab-arabi-2`.
- لا ترفع ملفات ZIP نفسها إلى GitHub.
- ارفع الدفعات بالترتيب 1 ثم 2 ثم 3.
- بعد اكتمال الدفعات، يجب أن تكون `index.html` و`api-worker.js` و`wrangler.toml` و`schema.sql` في جذر المستودع.

Worker التجريبي: `arabya-2-test`
قاعدة D1: `adab-arabi`

لا توجد أسرار أو كلمات مرور حساسة داخل المستودع.
